networks:
  fabric_esign:
    external: true

services:
  ca_tls:
    image: hyperledger/fabric-ca:1.5
    container_name: ca_tls
    environment:
      - FABRIC_CA_HOME=/etc/hyperledger/fabric-ca-server
      - FABRIC_CA_SERVER_CA_NAME=tlsca-esign
      - FABRIC_CA_SERVER_TLS_ENABLED=true
      - FABRIC_CA_SERVER_PORT=10054
      - FABRIC_CA_SERVER_CSR_CN=ca_tls
      - FABRIC_CA_SERVER_CSR_HOSTS=ca_tls,localhost
    ports:
      - "10054:10054"
    command: sh -c 'fabric-ca-server start -b admin:adminpw -d'
    volumes:
      - ./data-ca-tls:/etc/hyperledger/fabric-ca-server
    networks:
      - fabric_esign
